# this below configs will be fetched from git config server, so they are commented out here.
#jwt:
#  secretKey: LKJNALJRKGRLNARIWGANGLKAJGNARGUAOREGARWGJAUIAJINARLIGAERNGAERIGJ

spring:
  application:
    name: api-gateway
  config:
    import: optional:configserver:http://localhost:8888

# This below changes will be fetched from the config server, so they are commented out here. They are kept here for
#  cloud:
#    gateway:
#      server:
#        webflux:
#          routes:
#            - id: order-service
#              uri: lb://ORDER-SERVICE  # load-balanced URI for the order service
#              predicates:
#                - Path=/api/v1/orders/**
#              filters:
#                - StripPrefix=2
#                - name : LoggingOrderFilter
#                - name : Authentication  # or we can also use directly AuthenticationGatewayFilterFactory
#                  args :
#                    isEnabled : false
#            - id: inventory-service
#              uri: lb://INVENTORY-SERVICE
#              predicates:
#                - Path=/api/v1/inventory/**
#              filters:
#                - StripPrefix=2
#                - AddRequestHeader=X-Custom-Header, Chinuthon
#                - name: Authentication  #or we can also use directly AuthenticationGatewayFilterFactory
#
##server configuration
#server:
#  port: 8080
#
##eureka client configuration
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/

